import XCTest
@testable import CoordinatorPackage

final class CoordinatorPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CoordinatorPackage().text, "Hello, World!")
    }
}
